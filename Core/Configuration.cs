namespace TTT
{
    public class Configuration
    {
        public readonly float CellSize;

        public Configuration(
            float cellSize)
        {
            CellSize = cellSize;
        }
    }
}